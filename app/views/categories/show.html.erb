<h1><%= @category.name %></h1>
<%@category.items.each do |item|%>
<ul>
<h3><li><%= link_to item.name, item_path(item) %></li></h3>
<li><%= button_to "Add to Cart", add_to_cart_path, method: :patch, params: { item_id: item.id, name: item.name } %></li>
</ul>
<%end%>
